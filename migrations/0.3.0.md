# Migration to v0.3.0

## CLAUDE.md is now a managed file

`apply` generates `CLAUDE.md` from the template. Do not hand-edit it.

## beads version pinned

The devcontainer pins a specific `bd` version for reproducible builds.

### Steps
1. Rebuild your devcontainer to install the pinned beads.
