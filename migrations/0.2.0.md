# Migration to v0.2.0

## Devcontainer toolchain updated

Rust 1.84 → 1.92, `cargo-nextest` now built from source (avoids QEMU/arch
issues), and Go (`golang-go`) added for the beads build. `apply` regenerates the
managed `.devcontainer/Dockerfile`.

### Steps
1. Rebuild your devcontainer to pick up the new toolchain.
