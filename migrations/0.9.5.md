# Migration to v0.9.5

## Devcontainer br binary switched to musl

The devcontainer now installs the statically linked musl `br` build for broader
compatibility. `apply` regenerates the managed Dockerfile.

### Steps
1. Rebuild your devcontainer.
