# Migration to v0.9.8

## deburr-edge-cases skill replaced by de-complect

The `deburr-edge-cases` skill has been renamed and rewritten as `de-complect`. It now removes
complected (braided-together) concerns from design documents as well as implementation branches,
grounded in the simple-vs-easy distinction; the old ornamentation categories survive as instances
in its complect radar. `apply` generates the new skill at `.agents/skills/de-complect/SKILL.md`
but never deletes files, so the old skill file lingers in the target repo.

### Steps
1. Delete the stale skill: `rm -r .agents/skills/deburr-edge-cases`.
